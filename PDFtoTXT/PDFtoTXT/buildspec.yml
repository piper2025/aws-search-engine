version: 0.2

echo "TESTING AUTOMATIC UPDATE #2 - IT WORKS!"

phases:
  install:
    runtime-versions:
      python: 3.10
  build:
    commands:
      - echo "Starting the build..."
      - aws cloudformation package --template-file PDFtoTXT/PDFtoTXT/pdftotxt.yaml --s3-bucket search-artifacts-store --output-template-file lambdaoutput.yaml

artifacts:
  files:
    - lambdaoutput.yaml
    - PDFtoTXT/PDFtoTXT/pdftotxt.yaml

